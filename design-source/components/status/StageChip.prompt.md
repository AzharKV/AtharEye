See StatusPill.prompt.md — StageChip, StatusPill and SeverityDot are documented together in this directory.

```jsx
<StageChip stage="Early" />
<StatusPill status="On track" />
<SeverityDot severity="Major" />
```
